---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "common.yaml"
  - name: "Yaml backend 2"
    data_hash: yaml_data
    datadir: "data2"
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "common.yaml"
  - name: "Yaml backend 3"
    data_hash: yaml_data
    datadir: "data3"
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "common.yaml"
